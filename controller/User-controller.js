import User from "../model/User.js";

export const SignUP = async(req, res) => {
    const {name, email, password} = req.body;
    // const user = new User({
    //     name,
    //     email,
    //     password
    // })
    //await user.save();
}